# Website server
NODE_ENV=development
HOST=127.0.0.1
PORT=3000

# PostgreSQL connection
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=faelo_ai
DB_USER=postgres
DB_PASSWORD=GANTI_DENGAN_PASSWORD_POSTGRESQL_ANDA
DB_SSL=false

# Database used only by `npm run db:setup` to create DB_NAME when needed.
DB_ADMIN_DATABASE=postgres
